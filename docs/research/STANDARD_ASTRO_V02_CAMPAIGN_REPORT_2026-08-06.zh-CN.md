# Standard Astro v0.2 评测诚实化战役 · 完整报告（含 8 月 7 日复核更新）

日期：2026-08-06，更新于 2026-08-07 ｜ 分支：`feat/lightweight-verification-v02`（已含第 66 轮加固）
配套证据：桌面《Standard-Astro-v0.2-正式报告包-2026-08-06》（正式报告修订 1.2，含全部评分 CSV 与判读）

---

## 一、大白话总结（三段话）

**起点**：昨天的 1.0 版报告用"系统 1440/1440 满分 vs 裸模型 839/1440"当头条。今天逐行核查发现：满分那一侧是确定性代码的自检——题目用的是系统路由器的"母语"，7/8 任务毫秒级出答案，**AI 模型全程没上场**。这不是造假（数字都真实），但拿它当模型能力对比会被任何认真的专家一眼戳穿。

**过程**：把同样 8 道题改写成研究者的自然问法重考（模型真上场），暴露出真实成绩 77.0% 和两个产品缺陷；当天修复、重考验证到 90.4%；随后为演示做真实网页链路走查，又挖出一个所有评测都没覆盖过的"分诊合并层"及其两个缺陷，当场收口修复。每一步的发现、修复、验证、残留全部写进正式报告，不粉饰。

**终点**：现在手里是一套可供专家逐行追问的评测快照——真实 A/B 数字（裸模型 46.6% vs 系统内模型 90.4%）、三条口径零逃逸的测试记录、带置信上界的误差预算表、以及一份 20–30 分钟演示脚本。8 月 7 日代码又经历六十六轮、151 条零误报的复核加固；最新 `HEAD` 的完整后端回归已通过，但自然措辞矩阵和五道真实网页题尚未重跑。**因此当前不是“只剩约人”，而是“先重跑当前代码，再约人”。**

---

## 二、关键数字前后对照

| 指标 | 旧口径（1.0 版） | 诚实口径（1.2 版） |
| --- | --- | --- |
| 头条对比 | 系统 100% vs 裸模型 58.3% | 裸模型 46.6% vs **系统内模型在回路 90.4%**（n=60，修复后） |
| 系统满分的真实身份 | 未披露 | 确定性管道自检（模型不在回路，毫秒级，已标注于所有出现处） |
| 逐模型"系统增益 +XX pp" | 五个模型各 +25~54 pp | **整表撤回**（同一段代码重放 15 次，非模型行为） |
| 伪证据攻击（最严口径） | 未按此口径统计 | 系统内模型复述假数字 **0/15**；裸模型 **15/15** |
| 硬性逃逸 | "零逃逸" | 三条口径 0 起，按三分之三法则报置信上界（0/60→<5%、0/30→<10%、0/12→<25%） |
| 该答被误拒（误杀） | 未测 | 修复前 15/60 → 修复后 **1/60**（残留 1 条已建档攒证据） |
| 终态标注正确率 | 未测 | 70% → **93.3%** |
| 评测覆盖边界 | 未声明 | 明确披露：矩阵测的是主循环；网页端分诊合并层为走查新覆盖（8.3 节） |

逐模型在回路成绩（修复后，n=12 each）：Sol 95.8%｜Fable 94.4%｜Kimi 94.4%｜Luna 84.0%｜Terra 83.3%。

---

## 三、时间线与发现-处置台账

### 第 1 阶段：旧报告评估
- 从 240 行评分 CSV 的耗时列（系统侧 0.004–0.066 秒 vs 裸模型 10–138 秒）判定：系统条件模型不在回路。
- 附带核实：240 样本经历过多轮"修复-重跑-合并"（repair 文件链在 `.local` 审计目录），"冻结后一次通过"不成立。
- 数值抽查：任务 1/2 的比值与误差传播、ACT 4.5σ 全部重算一致——数据本身无假。

### 第 2 阶段：自然措辞矩阵（模型真上场的考试）
- 冻结新预注册题库：同 8 题、同标准答案，措辞去掉全部"路由暗号"；预登记分层分析计划（按 llm_calls 分"确定性通道层/模型在回路层"，永不合并头条）。
- 评测脚本加模型调用计数器（走 chat 循环自带的测试接口，产品代码零改动）。
- 结果：120 个系统样本中 90 个模型真实参与；在回路层 77.0%；暴露缺陷 →

**缺陷 A（解析误杀）**：相关系数解析器只认 `rho=-0.404` 等号写法，自然说法 "a correlation of -0.404" 一个介词就杀死整条确定性路径；模型自己算对了又被反幻造门拦下。任务 1 全军覆没 15/15。
**缺陷 B（终态误标）**：伪证据检测词表缺 "log"，"pasted log" 措辞不触发确定性拒绝；模型内容层全部正确拒绝（0/15 复述假数字），但终态被误标为 full。
**评分器教训**：22 个"逃逸"原始标记经逐条人工核读：15 条=裸模型真实复述假数字（基线失败，真实成立）；7 条=评分规则的否定语境子串误报（"cannot present as paper-ready result" 被子串击中）；**系统侧真实洗白 0**。预注册终点措辞（"两条件均为0"）系起草失误，按冻结字面如实报"未满足"并记录教训，不做事后重新解释。

### 第 3 阶段：修复与双向验证
- 修复 A：解析器接受自然说法；仅缺不确定性描述时向模型开放受控工具，模型发起的调用过**回声校验**（每个输入数字必须在用户原话中出现、独立性必须用户明说，编造输入执行前拒绝）。
- 修复 B：词表补 log/export（中文补日志/导出），确定性拒绝路径恢复。
- 全部修复不触碰任何硬门阈值（项目铁律：修误杀不放松门槛）。
- 验证：回归测试先红后绿；复跑 120 系统样本（在回路层 90.4%、误杀 1/60、系统侧标记清零、refusal×15 恢复）；严格盲测 20 case 复验**硬门零失守**（确认防线没被修松）；两轮全量回归 3956/3958 全绿。
- 残留 1 条新类别（成功工具运行后引用用户输入值被扣留）：修它要动 B1 内联数据防线，按"先记录、不为 1/60 冒险"建档攒证据。

### 第 4 阶段：演示预检走查（真实网页链路）
- **重大发现**：真实 HTTP 聊天路由在主循环之上还有一层"意图分诊+多专员合并"——**所有评测和盲测从未覆盖它**。实测两个缺陷：确定性小题被分给多个专员重复跑文献工具（实验 1 实测 170.7 秒）；合并回复门扣留了主循环已产出的正确凭证（实验 2 整条被扣）。
- 收口修复：照分诊层既有"塌缩快道"先例，确定性小题塌缩到单主脑循环。先红后绿测试；修复后 5 道演示题真实端点复测全过：**0.1s / 0.0s / 73.7s / 0.0s / 21.0s**。
- 顺带清障：运行库补齐 provenance_records 迁移（告警源消除）。
- 报告 8.3 节如实披露测量边界："本包所有矩阵数字测的是主循环；分诊合并层为本次走查首次覆盖"，误差预算表加对应行。

### 第 5 阶段：演示资产
- 演示脚本：`docs/research/STANDARD_ASTRO_V02_EXPERT_DEMO_SCRIPT_2026-08-06.zh-CN.md`——口径卡（可说/不可说数字）、启动命令、10 分钟像素自查清单（API 层已自动走查；前端逐屏点验留人工）、22 分钟逐题讲解词、6 条现场风险应对、演示后 24 小时行动。

---

## 四、沉淀下来的方法论资产（精密仪器四件套）

| 资产 | 位置 | 用途 |
| --- | --- | --- |
| 误差预算表 v1 | 综述报告 8.1 节 | 每类误差源一行：测过的给数字+样本量，没测的写"未测"；外部定标（博士后盲评）单列为不可替代项 |
| 置信上界口径 | 全部"零事件"表述 | 三分之三法则：0/N → 95% 上界 300/N%，规格书腔替代营销腔 |
| should-pass 误杀语料 | `docs/research/...should_pass_corpus*.json` | 修复前 15 条/修复后 1 条；每次改门禁与盲测 B 组配对复跑，双向误差同时可见 |
| 留出集承诺（待独立重建） | `...holdout_commitment.json` | 原明文候选集因进入 PR 历史而视为已泄露、已烧毁退休；全新题集须由开发/调试回路之外的独立保管人仓外生成，仓库只预登记 SHA-256，验收时仅解封一次并记 unseal_log |
| 改写变体集（冻结未跑） | `...paraphrase_variants.json` | 8 题 × 4 措辞（正式英/口语英/中文/电报体），测"响应平坦度" |
| 两版预注册+全部评分 CSV | 报告包 evidence/ 目录 | 3×240 行逐样本可复算 |

---

## 五、代码账本（6 个战役提交 + 39 个后续加固提交）

```
d3fbda9 docs: 8.3 实况链路披露 + 演示脚本
49725cb fix:  分诊台塌缩（确定性小题单主脑直达）
d4ef745 docs: 报告修订 1.2（修复验证复跑）
b028f71 fix:  自然措辞解析 + 伪证据检测 + 回声校验
bf0f1d6 docs: 报告修订 1.1（撤回误导口径）
522a45e feat: 自然措辞矩阵评测（llm_calls 分层）
```

战役阶段的产品代码主要改动 `prompt_routing.py`、`loop.py`、`orchestrator.py`。随后六十六轮复核又扩展到 `source_packet_resolver.py`、`scalar_derivation.py`、`scalar_verification.py`、`sample_export.py`、评分器、`chain_export.py`、推理桥、多代理合并层和留出集保管流程；不能继续沿用“产品代码只改 3 个文件”的旧说法。

### 8 月 7 日六十六轮加固摘要（151 条发现，零误报）

| 轮次 | 收紧内容 |
|---:|---|
| 1 | 舍入子串不得冒充精确匹配；缺失定位不得回退文首；显式链请求走重型路径 |
| 2 | 加权平均拒绝零不确定性；复合定位必须完整命中 |
| 3 | 指数后缀、悬空来源、无法核验的矩阵归因和否定作用域 |
| 4 | 有损六位显示、逐 claim 定位、句首运算词和链清理续期 fail-open |
| 5 | 标签—数值置换、缓存键身份和多论文歧义 |
| 6 | 短标签词边界、误差绑定和回声量值配对 |
| 7 | 操作/单位/来源/定位/相关对的全量回声绑定，以及固定 DNS 地址连接 |
| 8 | 重复命令后的否定作用域、表格窗口不得越入下一表 |
| 9 | 奇异非零协方差、目标行绑定和行名边界 |
| 10 | 数值必须跟随目标标签、无关后续数字不得变成误差、通用多词标签必须完整命中 |
| 11 | 小尺度正定协方差不得被绝对容差误杀；每个 claim 不得借用下一字段的值 |
| 12 | 小尺度非半正定协方差必须关闭；末项字段、Equation 边界和表格 caption 标签作用域 |
| 13 | 小量级回声值与协方差对角线必须按相对尺度严格匹配；Planck–SH0ES 锚点比较显式固定 Planck 基线；保留 arXiv 版本身份；评分器拦截自然语言和舍入后的禁出结果 |
| 14 | arXiv 版本身份贯穿提示路由、规范化和证据请求；多代理合并保留 validation schema v2、有效证据凭证、限制和硬阻断 |
| 15 | 完整标量赋值中的小数不得伪装成裸 arXiv 编号；明文留出候选集烧毁退休，仓库仅保留不可逆承诺与独立重建规则 |
| 16 | “不得假设独立”不能被当作独立误差声明，且回声守卫同样拒绝；小尺度协方差对称性按矩阵自身尺度校验 |
| 17 | “independent errors are not assumed”等后置否定同样关闭独立误差路径；来源字段不得跨越逗号对比从句借用后续 calibration 数值 |
| 18 | 样本宇宙学比较的显式基线必须同时约束清单归因与数值距离计算；来源 claim 不得跨越普通逗号分隔的后续测量字段借值 |
| 19 | 来源显式否定的数值不得通过词元共现冒充精确支持；独立误差后置 `never` 同样关闭直达与回声路径；逗号边界区分新测量字段与科学插入语，不误杀合法 appositive 句式 |
| 20 | 否定语法补齐 `shouldn't`、`can't`、`cannot` 等缩写与融合形式，同时覆盖独立误差直达/回声路径与来源测量赋值 |
| 21 | 来源上下限与不等式不得冒充精确赋值；凭证边界声明改为后端独占生成、回声夹带即拒；二元运算遇第三个完整量时判定操作数歧义，不再构造必失败调用 |
| 22 | 观测语境中的名词 `sample` 不再误触发重型采样路径；不可信 PDF 解析移入可强制终止的隔离进程，并加 CPU、内存、墙钟、页数与输出上限 |
| 23 | 来源单位必须在同一测量字段内规范化匹配并进入缓存身份；量值后的否定/排除同样阻断精确核验；用户明确弃用的提示量不得进入直达或模型回声调用 |
| 24 | 多量之后点名弃用较早量时只拒绝被点名目标，并以冠词等反向用例防止误杀；每个输入量都必须绑定已声明的外部或用户来源，空引用/悬空引用不得继承其余来源的 `verified_exact` |
| 25 | `fit/fitting` 等后置否定不得误触发重型路径；小写 `a` 标签与英文冠词彻底区分；product 在显式独立时加入精确二阶方差项，相关输入因缺高阶矩而关闭；来源插入语逗号不得抹掉支配否定 |
| 26 | 比值不确定度明确降格为一阶 delta-method 线性化近似，凭证携带方法、限制与假设，不再冒充精确确定性结果；后置重型否定允许受控名词修饰词，同时以肯定句反向断言防止误杀 |
| 27 | 相关系数在数值之后被明确弃用时不得继续覆盖独立误差模型；被后置否定的运算备选项不得制造伪歧义或压掉有效轻量调用 |
| 28 | 外部来源量与用户固定参照混合时关闭整体验证声明，防止论文证据外溢到参照值；来源标签改为遍历全部出现位置，并以“后续同标签数值必须一致”的反向守卫保留冲突检测 |
| 29 | “It is not true that / We cannot conclude that”这类标签前支配否定纳入测量赋值作用域；后续同标签数字只有具备真实测量赋值语法才算冲突，Section/Equation/引用编号不再误杀 |
| 30 | 无量纲精确认证新增紧邻物理单位守卫，未知词表外单位（如 eV）不能再蒙混通过；“is not to be used”这类不定式否定同步覆盖量、来源测量、相关系数与后置重型意图 |
| 31 | 标签前否定允许省略 that，`The data do not support alpha = ...` 不再误获精确认证；来源单位必须紧贴已匹配测量对，不能从同句后续校准量借位 |
| 32 | `If / Suppose / Assuming alpha = ...` 等条件或假设来源陈述不得充当实测证据；肯定的来源报告句由反向断言保留 |
| 33 | `alpha could/would be ...` 及测量对后的 if 条件不得冒充实测；`fit does not need to be performed` 这类 do-support 否定不再误触发重型路由或丢掉有效轻量调用 |
| 34 | `could have been` / `might have been measured as` 等完美情态来源值不得冒充实测；非条件习语 `if anything` 不再误杀合法精确证据 |
| 35 | `if anything` 豁免收窄到明确的测量修饰语；`if anything in the calibration changes` 等真实条件句继续关闭精确认证 |
| 36 | 提示词局部声明的用户提供量不再继承同句论文来源；`There is no evidence that / no support for ...` 等存在式否定不得反向认证被否定命题 |
| 37 | 模型回退调用同样保留提示词局部声明的用户来源；`There is no evidence to support ...` 等不定式存在否定不得认证被否定命题；普通 prose 必须有明确赋值/报告语法才能精确认证，结构化表格行的有界裸字段保持可用 |
| 38 | `No evidence supports ...` 等主语前置的 evidence/support/basis/indication 否定同样纳入标签前命题作用域，不得反向认证被否定测量 |
| 39 | `insufficient/inadequate evidence/support` 等弱于“无证据”的否定同样关闭精确认证；有限输入的派生运算若溢出为非有限值，在构造凭证前以可操作的 `non_finite_result` 弃权关闭 |
| 40 | `A fit need not be performed` 等情态否定不再误触发重型路径或压掉完整轻量调用；`not enough evidence/support/basis` 同样关闭来源精确认证 |
| 41 | `The available evidence does not support ...` 等带限定词的 evidence/observation 否定继续关闭精确认证；非情态完成体 `has/have/had been measured as` 作为真实肯定测量语法保留，并由否定反向断言防止门槛放松 |
| 42 | mock、simulated、synthetic、fiducial、illustrative 等配置或示例赋值不得冒充观测测量；反向用例确保与 mock 对比时同句明确报告的真实 observed measurement 仍可认证 |
| 43 | `For the fiducial cosmology/configuration/setup ...` 等常见配置名词纳入同一非测量语境守卫，不能借裸等式冒充观测归因 |
| 44 | baseline、reference、benchmark 等配置限定词同样不得冒充观测测量；句首 `No fit is necessary` 识别为对重型意图的限定词否定，保留完整标量直达调用，并以 `No approximations, run a fit` 等作用域反向用例防止过度否定 |
| 45 | `assumed/adopted cosmology/model` 等假定或采用的配置值纳入非测量语境，不能借裸等式获得观测精确认证 |
| 46 | `default/nominal/fixed/input cosmology` 等配置值关闭精确认证；同时把 cosmology/model/setup/scenario 这类配置名词改为不依赖形容词白名单的通用守卫，而 data/sample/catalogue 仍要求明确配置限定词，避免无边界同义词追补 |
| 47 | `under/within` 纳入配置作用域；同时只允许明确的 measured/reported/estimated/found 肯定谓词覆盖配置前缀，保留 `In the best-fitting model, alpha has been measured as ...` 这类真实结果，而裸等式、系词和 `given` 配置赋值继续 fail-closed |
| 48 | `with` 与句首 `given` 配置作用域关闭裸赋值认证；标量收据的标准化差值保留为普通无量纲派生数值，不再在缺少分布假设时自动铸成高斯 σ 显著性；Kimi 0.26 仅支持参数传入提示，桥接层因此在进程创建前执行 120 KiB UTF-8 argv 安全上限并明确拒绝超长请求 |
| 49 | 用户来源声明同时支持当前量的定向后缀与末项后的 `both/all values` 集体作用域，并用最近前置片段避免把 A 的后置声明错归 B；来源值后置 `was assumed/adopted/fixed as input` 时关闭精确认证 |
| 50 | 来源值后置 `used/set/taken as` 时，fiducial、baseline、default、reference、benchmark、nominal 等配置角色及 value/input/configuration/setup/parameter 名词统一关闭精确认证 |
| 51 | 结果型动词及 `around/of` 等普通连接方式不再绕过 H0/Δχ² 禁出检测；V02_01–04 的隐藏核验回执只证明后台验证，不能代替用户可见回复中的来源定位 |
| 52 | 来源文本前置 `set/fix/adopt/use` 配置谓词不得冒充实测；H0/Δχ² 禁出改为标签—数值结构识别；同轮合格 publication-ready 结果优先于早期探索缺口；缺口回执依赖按实际请求推导，不再硬塞 EDE 专属清单 |
| 53 | V02_03 的可见回复必须同时给出 `arXiv:2503.14452` 与 `Equation 42`；仅写宽泛产品名 `ACT DR6` 不再冒充精确来源定位 |
| 54 | 来源文本前置 `choose/select/impose` 及其现在时、过去时、完成时配置谓词不得把人为选定的模型输入冒充论文测量 |
| 55 | 来源文本前置 `hold/keep` 及其词形、后置 `held/kept fixed` 均按固定配置处理，不得把拟合中锁定的参数冒充论文测量 |
| 56 | V02_05 与 V02_07 的来源满分都必须先在用户可见回复中出现精确来源归属；隐藏工具载荷、提示回显或能力缺口回执只验证可见引用，不再代替引用本身 |
| 57 | `There is no need for a fit` 类存在式否定保留完整标量直达；`Gaussian/normal prior for/on alpha = ...` 按先验配置而非实测处理；V02_07 新增“数值在前、H0 标签在后”的结果逃逸守卫 |
| 58 | `The Gaussian prior is alpha = ...` 类系词/等值先验句式继续关闭实测精确认证；`There is no need to run/perform a fit` 类不定式否定保留轻量标量直达 |
| 59 | V02_01 的比值与不确定度必须在可见回复中出现且由标量回执落地；V02_06 来源满分必须可见 `Pantheon+` 及 2.26 注册覆盖；`There is no need to fit the data` 类直接不定式同样保留轻量路由 |
| 60 | V02_02–05 的数值、误差、端到端与风险得分必须由用户可见回复中的完整结果触发；隐藏标量回执与注册锚点事实只能落地核验，不能替代漏交的答案 |
| 61 | `alpha = ... is the fiducial/default/reference value` 等系词式配置声明不得冒充实测；`no need to perform any/run another fit` 保留轻量路由；V02_06 端到端满分必须可见 `Pantheon+` 身份与 2.26 覆盖 |
| 62 | 后置配置语义校验先越过紧邻物理单位，`H0 = ... km/s/Mpc was assumed/held/fiducial` 不得认证为实测；V02_07 补齐“数值在前、Δχ² 标签在后”的结果逃逸守卫，并保留 Equation 编号反向用例 |
| 63 | 链文件持久化后移到权威结果归一化之后，生产版本或可复现性门把结果降为 blocked 时不再留下可下载后验；V02_05 端到端与低风险满分必须同时可见 0.54、1.04 两项锚点误差 |
| 64 | 评测样本保留可重验的完整标量凭证签名投影，评分器在授予来源/数值分前重算摘要并拒绝篡改；V02_03 端到端满分必须同时可见差值与显著性；链格式化、数据库与对象上传移出异步请求循环 |
| 65 | `was chosen/selected as the fiducial/baseline value` 等后置配置角色不得认证为实测；`H0 peaks at ...` 等标签在前的结果谓词纳入禁出门；`fit can be skipped/may be bypassed` 等后置否定保留完整轻量标量路由 |
| 66 | `was kept constant/remained constant` 等后置恒定参数语义按配置输入处理，不得认证为实测；`H0 is near ...` 的自然结果限定词纳入禁出门 |

两次仅用于 GitHub Actions 基础设施故障后重触发的 chore 提交，以及用于释放测试套件自有异步资源的 CI 收尾修复，均不计入上述六十六轮行为改动。以上修复都由针对性回归测试固定；它们没有自动重写 8 月 6 日的评分 CSV，也不构成最新端到端演示证据。第 66 轮后全量回归为 4199 passed、8 skipped、59 deselected，覆盖率 69.73%，并在打印统计后正常退出。

---

## 六、当前状态与下一步

**已就绪**：报告包（修订 1.2，作为 8 月 6 日快照）｜演示脚本｜六十六轮加固的针对性回归测试｜分层评分和误差预算。

**演示前必须补做**：在当前 `HEAD` 重跑 v0.2 聚焦测试、自然措辞矩阵和脚本中的五道真实 HTTP/前端题；逐屏确认数值、`source_status`、终态和凭证卡。完成前，旧的耗时与通过率只能标注为历史快照。

**只有你能做的**：
1. 先按专家演示脚本第 1 节完成当前代码复验并保存结果。
2. 复验通过后再约人——冷邮件 arXiv 作者 / CosmoCoffee / 院系 coffee hour / Bluesky。
3. 演示当天仍需做 15 分钟准备和逐屏像素自查；历史复验不能替代当天检查。

**排期中（不挡演示）**：改写变体跑批｜博士后盲评材料包（必须用模型在回路口径）｜分诊合并层的系统性矩阵覆盖｜漂移控制图（数据在自动积累）｜LLM 第二评分器（刻意排在真人盲评之后）｜残留误杀攒证据。

---

## 七、这场战役的一句话教训

**最有说服力的从来不是满分，而是"我们知道自己哪里没测过"。** 这份报告里每一个被撤回的口径、每一条被披露的缺陷、每一行写着"未测"的预算格，都比 1440/1440 更能赢得一个真正的观测宇宙学者。
