name: Daily

# Daily blind tests + integration tests. NOT part of the per-PR gate
# (those live in ci.yml). Triggers at 16:00 UTC (~00:00 Beijing) and
# can be manually fired via workflow_dispatch.
#
# Requirements (set in repo Settings → Secrets and variables → Actions):
#   ANTHROPIC_API_KEY   — for the blind-test LLM calls (any provider key
#                          recognized by app.ai.inference_router will work;
#                          this is just the default)
#
# If no secret is configured the blind-test job is skipped (the if-guard
# below) but integration tests still run. That keeps the daily cron from
# spuriously red-X-ing the repo health badge while keys are being set up.

on:
  schedule:
    - cron: "17 16 * * *"     # 16:17 UTC daily (off the top-of-hour: GitHub silently drops congested whole-hour crons — 2026-06-11's run was skipped, and actual starts had drifted to +2.4h)
  workflow_dispatch:           # manual button in the Actions UI
    inputs:
      module:
        description: "Module to run. cosmology is the only active module in this repo."
        required: false
        default: "cosmology"
        type: choice
        options:
          - cosmology
      cases:
        description: "Comma-separated case IDs to run (e.g. A2,A3). Empty = all cases in the module(s). Tight iteration loops use this to shrink a 20-min run to 1-2 cases × ~2 min each."
        required: false
        default: ""
        type: string
      provider:
        description: "LLM provider for the runner. 'auto' picks deepseek when only DEEPSEEK_API_KEY is set (the cheap daily-cron path), otherwise lets the runner default to anthropic."
        required: false
        default: "auto"
        type: choice
        options:
          - auto
          - anthropic
          - deepseek
      deepseek_profile:
        description: "DeepSeek model profile when provider=deepseek or auto-picked deepseek. v4-pro runs with thinking enabled (the cron default); v4-flash is the non-thinking fallback."
        required: false
        default: "deepseek:v4-pro"
        type: choice
        options:
          - deepseek:v4-pro
          - deepseek:v4-flash
      model:
        description: "Claude model id when provider=anthropic (claude-opus-4-7 or claude-sonnet-4-6). Ignored when provider=deepseek or auto-picked deepseek."
        required: false
        default: "claude-sonnet-4-6"
        type: choice
        options:
          - claude-sonnet-4-6
          - claude-opus-4-7

jobs:
  blind-tests:
    runs-on: ubuntu-latest
    # GitHub Actions does NOT allow the `secrets` context in a job-level
    # `if:` (only in step `if:` and `env:`), so the activation gate is the
    # vars.DAILY_BLIND_ENABLED flag alone. If the user opts in via that
    # variable but forgot to set the API-key secret, the runner aborts
    # cleanly on missing-key — daily_blind.sh + runner.py both check.
    if: ${{ vars.DAILY_BLIND_ENABLED == 'true' }}
    timeout-minutes: 45
    # contents:write is required only by the evidence-log publish step at the
    # end of this job; the token cannot touch secrets or other repos.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.lock
      - name: Install dependencies
        run: |
          cd backend
          python -m venv venv
          ./venv/bin/pip install --require-hashes -r requirements.lock
      - name: Initialize blind-test database
        run: |
          cd backend
          # Blind cases exercise the durable research-record path.  Export one
          # runner-temporary URL both to this shell and every following step so
          # migrations and the runner cannot accidentally use different files.
          export DATABASE_URL="sqlite+aiosqlite:///$RUNNER_TEMP/daily-blind.sqlite3"
          echo "DATABASE_URL=$DATABASE_URL" >> "$GITHUB_ENV"
          ./venv/bin/alembic upgrade head
          ./venv/bin/alembic check
      - name: Run blind tests (cosmology)
        env:
          # Map PLATFORM_DEEPSEEK_API_KEY (platform-default secret name)
          # onto the DEEPSEEK_API_KEY env var the runner actually reads,
          # so a single PLATFORM_DEEPSEEK_API_KEY secret is enough to
          # activate the deepseek path.
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY || secrets.PLATFORM_DEEPSEEK_API_KEY }}
          # cosmology is the only active module; the cron has no inputs, so the
          # `|| 'cosmology'` fallback applies. daily_blind.sh honors --module.
          BLIND_MODULE: ${{ inputs.module || 'cosmology' }}
          BLIND_CASES: ${{ inputs.cases || '' }}
          BLIND_PROVIDER: ${{ inputs.provider || 'auto' }}
          # Read by runner.py when the deepseek provider is active.
          BLIND_DEEPSEEK_PROFILE: ${{ inputs.deepseek_profile || 'deepseek:v4-pro' }}
          # CLAUDE_MODEL is read directly by app.ai.model_profiles when
          # provider=anthropic; ignored otherwise.
          CLAUDE_MODEL: ${{ inputs.model || 'claude-sonnet-4-6' }}
        run: |
          set -- --module "$BLIND_MODULE"
          if [[ -n "$BLIND_CASES" ]]; then
            set -- "$@" --case "$BLIND_CASES"
          fi
          if [[ "$BLIND_PROVIDER" != "auto" ]]; then
            set -- "$@" --provider "$BLIND_PROVIDER"
          fi
          bash backend/scripts/daily_blind.sh "$@"
      - name: Upload blind-test results
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: blind-tests-${{ github.run_id }}
          path: |
            backend/scripts/blind_test_cosmology_m0/results_*
          if-no-files-found: ignore
      - name: Publish evidence log
        # Append this run's mechanical summary + per-file hashes to the
        # append-only `evidence-log` branch, so blind-test outcomes remain
        # externally checkable after the 90-day artifact retention expires.
        # Full transcripts (case_*.json) are NOT published — only their
        # sha256, so a later dispute can still be settled against the
        # artifact while hidden-answer material stays out of git history.
        # This is a git audit log, not a cryptographic transparency log;
        # append-only is enforced by branch protection (see
        # docs/HONESTY_EVIDENCE.md). A publish failure fails the job on
        # purpose: a silent gap in the log would look like a covered day.
        if: always()
        env:
          RUN_ID: ${{ github.run_id }}
          RUN_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          shopt -s nullglob
          results=(backend/scripts/blind_test_cosmology_m0/results_*)
          if [[ ${#results[@]} -eq 0 ]]; then
            echo "no results directory produced; nothing to publish"
            exit 0
          fi
          src="${results[${#results[@]}-1]}"
          day="$(date -u +%Y-%m-%d)"
          dest="log/${day}-${RUN_ID}"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git fetch origin evidence-log 2>/dev/null; then
            # FETCH_HEAD is per-worktree: only the checkout that ran the fetch
            # has it, so resolve it to a commit BEFORE the new worktree exists.
            # Reading FETCH_HEAD inside the worktree failed every scheduled run
            # from 2026-09-05 to 2026-09-08 ("'FETCH_HEAD' is not a commit").
            evidence_head="$(git rev-parse --verify FETCH_HEAD)"
            git worktree add -B evidence-log /tmp/evidence-log "$evidence_head"
          else
            git worktree add --detach /tmp/evidence-log
            (cd /tmp/evidence-log && git checkout --orphan evidence-log && git rm -rf --quiet . || true)
          fi

          if [[ -e "/tmp/evidence-log/$dest" ]]; then
            echo "log entry $dest already exists; idempotent skip"
            exit 0
          fi
          mkdir -p "/tmp/evidence-log/$dest"
          if [[ -f "$src/summary.md" ]]; then
            cp "$src/summary.md" "/tmp/evidence-log/$dest/summary.md"
          fi
          (cd "$src" && find . -type f -exec sha256sum {} \; | sort -k2) \
            > "/tmp/evidence-log/$dest/hashes.txt"
          {
            echo "run_id: $RUN_ID"
            echo "commit: $RUN_SHA"
            echo "published_at: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo "artifact: blind-tests-$RUN_ID (90-day retention)"
          } > "/tmp/evidence-log/$dest/meta.txt"

          cd /tmp/evidence-log
          git add "$dest"
          git commit -m "evidence-log: $day run $RUN_ID"
          for attempt in 1 2 3; do
            if git push origin evidence-log; then
              exit 0
            fi
            git pull --rebase origin evidence-log || true
          done
          echo "failed to push evidence log after 3 attempts" >&2
          exit 1

  integration:
    # Integration tests are the pytest cases marked `integration and not slow`
    # (~44 cases): tests/test_e2e_full.py end-to-end execute_tool() coverage
    # (data-query / science / sandbox / literature+export / spectrum-image-VO /
    # pipeline / session / collaboration tools) plus three live-connector
    # checks in tests/test_connectors.py (SIMBAD, SDSS, Gaia). Many are
    # @pytest.mark.network and hit live archive TAP services, so an upstream
    # outage (e.g. SIMBAD 503) can redden this job without any code
    # regression. Pure pytest, no LLM key needed.
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.lock
      - name: Install dependencies
        run: |
          cd backend
          pip install --require-hashes -r requirements.lock
      - name: Run integration tests
        run: |
          cd backend
          # Disable coverage gate: integration is a small slice (~44 cases) so
          # its coverage (~19%) trivially fails pytest.ini's 45% threshold,
          # which is calibrated for the unit-test suite in backend-test.
          python -m pytest tests/ -v --tb=short -m "integration and not slow" --no-cov
      - name: Audit citation-pool reachability
        # 2026-06-13 (P3b): audit_registry runs per-PR, but the citation-pool
        # reachability audit (the SPECIFICITY side that guards against
        # anchor-gate false kills of the 9f2667e class) never ran on a
        # schedule — this campaign alone pinned 20+ new citations that had
        # never passed an automated reachability check. Daily cadence here;
        # a regression turns the Daily badge red without gating PRs.
        run: |
          cd backend
          python scripts/audit_citation_pool.py

  cobaya-parity:
    # The 4 cobaya-parity honesty locks: our reimplemented BAO likelihoods
    # checked for -2·logp parity against the REAL cobaya likelihoods
    # instantiated on the same released files —
    #   tests/test_eboss_dr16_grid_bao.py::test_parity_against_real_cobaya_likelihood
    #     (3 grids: eboss_dr16_elg_bao, eboss_dr16_lyauto_bao, eboss_dr16_lyxqso_bao)
    #   tests/test_sdss_dr12_consensus.py::test_parity_against_real_cobaya_likelihood
    # These skipif on the cobaya bao_data checkout under backend/packages/,
    # which is gitignored — so they silently SKIP in every ci.yml job and
    # this is the only place in CI where they actually execute. They are
    # honesty locks: no continue-on-error, parity drift must be loud red.
    # Kept out of ci.yml on purpose (PR gate stays light).
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.lock
      - name: Install dependencies
        run: |
          cd backend
          # The universal lock already pins cobaya==3.6.2 and camb==1.6.6,
          # the exact parity baseline, with hashes for every wheel/sdist.
          pip install --require-hashes -r requirements.lock
      - name: Fetch cobaya bao_data checkout
        # backend/packages/ is gitignored (backend/.gitignore), so CI must
        # download the released BAO data files that the real cobaya
        # likelihoods are instantiated on. Same command as the local
        # provisioning documented in scripts/cobaya/.
        run: |
          cd backend
          cobaya-install bao.sdss_dr12_consensus_bao bao.sdss_dr16_bao_elg bao.sdss_dr16_baoplus_lyauto bao.sdss_dr16_baoplus_lyxqso -p packages
      - name: Verify parity data present (anti-silent-skip guard)
        # The parity tests skipif on these exact files. If the download step
        # ever silently changes layout or fails soft, this step fails loud
        # instead of letting the honesty locks quietly go back to SKIPPED.
        run: |
          cd backend
          test -f packages/data/bao_data/sdss_DR16_ELG_BAO_DVtable.txt
          test -f packages/data/bao_data/sdss_DR16_LYAUTO_BAO_DMDHgrid.txt
          test -f packages/data/bao_data/sdss_DR16_LYxQSO_BAO_DMDHgrid.txt
          test -f packages/data/bao_data/sdss_DR12Consensus_bao.dat
          sha256sum -c scripts/cobaya/bao_data.sha256
      - name: Run cobaya parity tests
        run: |
          cd backend
          python -m pytest "tests/test_eboss_dr16_grid_bao.py::test_parity_against_real_cobaya_likelihood" "tests/test_sdss_dr12_consensus.py::test_parity_against_real_cobaya_likelihood" -v --no-cov
