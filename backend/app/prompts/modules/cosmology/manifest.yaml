# Cosmology module manifest (M1 Phase 2, 2026-05-18)
# ACTIVE — the only module loaded under ASTRO_RESEARCH_FOCUS=cosmology.
name: cosmology
display: Observational Cosmology
status: active
prompt_file: prompt.md

# Tools loaded for this module = core (via core/infrastructure.yaml)
#                              + cosmology-specific (28)
#                              + overlap (15, shared with N+1 modules)
# Total: 18 + 28 + 15 = 61 visible tools (test_module_manifest pins this).
tools:
  # ── cosmology-specific (28) ──
  - assess_bao_bin_anomaly
  - audit_published_constraint
  - build_cosmology_likelihood
  - build_cosmology_robustness_matrix
  - build_evidence_graph
  - compare_luminosity_distances
  - compute_theory_cmb_spectrum
  - demagnify_sample
  - estimate_photo_z
  - evaluate_chain_diagnostics
  - export_research_report
  - export_sample_table
  - fit_cosmology_mcmc
  - fit_line_lfr
  - get_cosmology_run_status
  - list_cosmology_datasets
  - load_cosmology_data_product
  - plan_research_program
  - prepare_spectral_measurements
  - run_cobaya_cosmology
  - run_cmb_rotation_likelihood
  - run_cosmology_likelihood_chain
  - run_cosmology_robustness_matrix
  - run_dark_energy_evidence_matrix
  - run_nested_sampler
  - run_research_matrix
  # Stable signed-workflow entry points. New formal compositions remain data
  # in the Registry and do not add another model tool name.
  - discover_registered_workflows
  - start_registered_workflow
  # spot_check_literature_value removed (2026-05-20): real cosmology H0/sigma8/S8 tensions
  # were being flagged as paper fabrication at +/- 3sigma; false positives outweighed the value.
  # extract_paper_measurements_with_llm folded into fit_line_lfr (2026-05-20): fit_line_lfr now
  # accepts arxiv_id and internally runs LLM extraction + +/- 1% cell verification.
  # classify_literature_relevance / verify_research_facts moved to overlap (2026-05-20):
  # the literature anti-fabrication layer is focus-agnostic and will be reused by N+1 modules.
  # -- overlap (15) -- shared with future active modules --
  - analyze_cross_wavelength
  - analyze_spectrum
  - analyze_spectrum_pro
  - classify_literature_relevance  # Stage 6 P0c-C: hard-gate secondary filter, focus-agnostic anti-fabrication
  - classify_transient
  - compute_galaxy_sfr
  - generate_proposal
  - get_followup_recommendation
  - query_gaia_cluster
  - query_high_velocity_stars
  - query_transients
  - research_workflow
  - search_lightcurve
  - verify_research_facts  # focus-agnostic fact verification, reused by N+1 modules
  - verify_scalar_derivation  # v0.2 controlled scalar derivations; dark-launched by feature flag
datasets:
  # TBD M1 Phase 3 — pull from app.services.cosmology_likelihoods DATASET_REGISTRY.
  # Will include: desi_dr1_bao, planck2018_compressed, act_dr6, pantheon_plus,
  #               des_sn_5yr, union3, h0licow, etc.
